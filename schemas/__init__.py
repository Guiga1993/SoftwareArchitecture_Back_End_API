"""Define Pydantic request, query, and response contracts for the backend API.

Modules in this package validate data at the HTTP boundary and describe the
OpenAPI shapes exposed to clients. Database mappings remain in ``model`` and
business operations remain in ``services``.
"""
